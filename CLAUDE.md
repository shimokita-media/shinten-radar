# 新店レーダー — プロジェクト指示

## これは何か

世田谷区の営業許可オープンデータから、下北沢エリアの**新店と閉店を毎月拾う**ツール。
運営者は下北沢のローカルメディア「シモキタメディア」（Instagram: @media_shimokita）。
抽出結果をもとに実際に店へ足を運び、取材してReelsを作る。

出力は人間が現地確認する前提の**候補リスト**。断定する道具ではない。

## 構成

```
shinten_radar.py   新店を拾う（メイン）
heiten_radar.py    閉店を拾う（--expired が主。引数なしは版間差分）
timeline.py        開店・閉店の年表
build_map.py       地図HTML
chains.txt         チェーン店キーワード
run_monthly.bat    月次実行（毎月16日）
archive/           旧システム（厚労省IFAS版）。現行とは無関係
```

依存は `requests` だけ。pandas は使わない。

## データソース

世田谷区 https://www.city.setagaya.lg.jp/02245/online_tetsuzuki/3246.html

| | URL | 文字コード | 更新 |
|---|---|---|---|
| 新規許可施設一覧 | `.../3246/shinkir{和暦YYMMDD}.csv` | **UTF-8 BOM付き** | 毎月15日ごろ |
| 全許可施設一覧 | `.../3246/zenkenr{和暦YYMMDD}.csv` | **cp932** | 4/20・10/15ごろ |

ファイル名の日付は新規許可＝その月の**末日**、全許可＝スナップショット日。
**どちらも直近ぶんしか置かれていない。** 過去版は404になるので、
`data/shinki/` と `masters/` のキャッシュを消さないこと。消すと過去が復元できない。

## 判定ロジックの根拠（実データで確認済み）

**新店と更新の見分け** — 許可開始日が月の**1日**の行は更新の可能性が高い。
更新許可は前許可の満了翌日に始まり、満了日は月末に寄るため。
令和8年7月分99行のうち48行が1日開始に集中していた。

`初回許可日` 列は新規許可一覧では**常に許可開始日と同じ**。新店/更新の判別には使えない。

**閉店の検出** — 全許可施設一覧に満了切れの施設は1件も残らない（更新しないと消える）。
一方、更新した店は翌月の新規許可一覧に1日開始で再登場する。
よって「満了後、新規許可一覧に戻ってこない店」＝閉店の可能性。
令和8年6月30日満了の11軒で検証：8軒が更新として再登場、3軒が消えた。

判定は許可単位ではなく**店単位**。1軒が飲食店営業と菓子製造業を別々に持つことがあり、
有効な許可が1つでも残っていれば営業中とみなす。

**初回許可日（全許可一覧のほう）** — エリア923軒中132軒に1962〜2015年の実際の初回日が入る。
残りは現許可がそのまま初回として記録されている。営業者や屋号が変わると記録が新しく始まるので、
**営業年数は下限**として扱う。

## 絶対に守ること

1. **業態を推測で埋めない。** データにあるのは屋号・住所・許可日・業種区分だけ。
   「ラーメン店」「居酒屋」はデータに存在しない。出力に業態を書かない。

2. **日付を断定しない。** 許可日からわかるのは範囲だけ。
   - オープンは許可開始日が**下限** →「◯日**以降**」
   - 閉店は許可満了日が**上限** →「◯日**までに**」
   - 許可から実際の開店までのラグは店によってバラバラ。踏み込んで「◯日ごろ」と書き換えない。

3. **「跡地」は方書まで一致したときだけ。** 同じ番地に別のビルが建っていたり、
   同じビルの別の階だったりする。判定は `shinten_radar.same_unit()`。
   番地しか一致しないものは「参考」止まりにする。過去に4件誤判定した。

4. **食べログ・Instagram をスクレイピングしない。** 裏取り支援は検索URLの生成まで。

5. **廃業は反映されないことを出力に明記する。** 許可済み＝営業中ではない。

6. **屋号が空の行は落とす。** 自宅営業の個人情報配慮。精度向上の名目で外さない。

7. **出典表示を出力に含める。** 出典：「新規許可施設一覧」（世田谷区）

8. **推測でコードを書かない。** 列名・文字コード・件数は実データを見てから決める。
   実際、当初の仕様書は「Shift-JIS」としていたが新規許可一覧はUTF-8だった。

## コード方針

- 設定値（エリア、チェーンリスト、色）はファイル冒頭の定数か外部ファイルに置く
- 文字コードは utf-8-sig / cp932 / utf-8 / euc_jp を順に試し、当たったものをログに出す
- 和暦は令和・平成・昭和・元年をすべて吸収する（`parse_wareki`）
- 日付はISO形式に揃えて持ち回る。スクリプト間で形式が食い違うと後段が落ちる
- 変更したら実データで走らせてから完了報告する

## 地図のデザイン方針

黄と黒。下北沢の小劇場の立て看板・ライブハウスのフライヤーの配色。

- ピンは色だけでなく**形**で見分ける。黄の塗り＝新店／黒の塗り＝チェーン／**破線**＝閉店の可能性
- 地図の下地は脱色して紙色に寄せる。街路名が読める濃さは残す
- 書体は Zen Kaku Gothic New（見出しは900）。**ドット書体(DotGothic16)で日本語を組まない。**
  漢字が点に潰れて読めない。台帳の日付の数字専用と考える
- 情報を足すときは、既にある表示と重複しないか確認する。
  重複した飾り（バッジ等）は落とす
