# 毎月16日の自動実行（Windows タスクスケジューラ）

世田谷区の「新規許可施設一覧」は毎月15日ごろ更新されるので、翌16日に走らせる。

## 1. LINE の準備

1. https://developers.line.biz/console/ でプロバイダを作り、**Messaging API チャネル**を新規作成
2. 「Messaging API設定」タブ → **チャネルアクセストークン（長期）** を発行してコピー
3. 同じページの QRコードから、**自分のLINEでそのBotを友だち追加**（これをしないと push が届かない）
4. 「チャネル基本設定」タブ → **あなたのユーザーID**（`U` から始まる33文字）をコピー

## 2. .env を置く

`.env.example` をコピーして `.env` を作り、値を入れる。

```bash
copy .env.example .env
```

```
LINE_CHANNEL_ACCESS_TOKEN=（手順1-2でコピーしたトークン）
LINE_TO_USER_ID=Uxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

`.env` は `.gitignore` に入れてあるのでコミットされない。

## 3. 手で1回テスト

```bash
python shinten_radar.py --last 1 --notify
```

LINEに届けば成功。届かない場合は、
- `[通知スキップ]` と出る → `.env` が読めていない（ファイル名が `.env.txt` になっていないか確認）
- `[通知失敗] HTTP 400` → ユーザーIDが違う
- `[通知失敗] HTTP 403` → Botを友だち追加していない

## 4. タスクスケジューラに登録

### GUI で登録する場合

1. スタートメニューで「タスク スケジューラ」を起動
2. 右ペインの **「タスクの作成」**（「基本タスク」ではなく、こちら）
3. **［全般］**タブ
   - 名前: `新店レーダー 月次`
   - 「ユーザーがログオンしているかどうかにかかわらず実行する」を選択
   - 「最上位の特権で実行する」はチェック不要
4. **［トリガー］**タブ →「新規」
   - 「タスクの開始」: **スケジュールに従う**
   - **月単位** を選択
   - 月: すべてチェック
   - 日: **16**
   - 開始時刻: `10:00:00`
5. **［操作］**タブ →「新規」
   - 操作: プログラムの開始
   - プログラム/スクリプト: `C:\Users\take0\shinten-radar\run_monthly.bat`
   - 開始（作業フォルダー）: `C:\Users\take0\shinten-radar`
6. **［条件］**タブ
   - 「コンピューターをAC電源で使用している場合のみタスクを開始する」の**チェックを外す**（ノートPC対策）
7. **［設定］**タブ
   - 「スケジュールされた時刻にタスクを開始できなかった場合、すぐにタスクを実行する」を**チェック**
     （PCが落ちていた月に取りこぼさないため）
8. OK → Windowsのログインパスワードを求められたら入力

### コマンドで登録する場合

管理者権限の PowerShell で1行。

```powershell
schtasks /create /tn "新店レーダー 月次" /tr "C:\Users\take0\shinten-radar\run_monthly.bat" /sc monthly /d 16 /st 10:00 /f
```

登録の確認と手動実行:

```powershell
schtasks /query /tn "新店レーダー 月次" /v /fo list
schtasks /run /tn "新店レーダー 月次"
```

## 5. 動作ログ

`out\run.log` に追記される。うまく動かないときはここを見る。

## 通知の中身

- 送るのは **初出の新店候補だけ**。「更新とみられる」は送らない
- 初出かどうかは `state\seen.json` で管理している。全部やり直したいときはこのファイルを消す
- 1件も無い月は「今月は新店なし」とだけ届く
- 1通あたり 屋号 / 住所 / 許可開始日 / 地図リンク の4点のみ
